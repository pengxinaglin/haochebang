package com.haoche51.checker.test;

import android.test.AndroidTestCase;

public class TestVehicleService extends AndroidTestCase {
//	public void testSyncVehicleData() {
//		long startTime = System.currentTimeMillis();
//		assertEquals(147, VehicleBrandDAO.getInstance().getCount());
//		assertEquals(1504, VehicleSeriesDAO.getInstance().getCount());
//		assertEquals(23634, VehicleModelDAO.getInstance().getCount());
//		long cost = System.currentTimeMillis() - startTime;
//		Log.e("test", "cost: " + cost/1000 + "s");
//	}
}
